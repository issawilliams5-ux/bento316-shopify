import Link from 'next/link';import { demoPacks } from '@/lib/demo';import { UsageCard, EmptyState } from '@/components/Cards';
export default function Dashboard(){return <div><div className="flex flex-wrap items-center justify-between gap-4"><div><h1 className="text-3xl font-bold">Welcome back</h1><p className="text-slate-300">Create, organize, and reuse product ad packs.</p></div><Link href="/dashboard/new" className="rounded-xl bg-electric px-5 py-3 font-bold">New Ad Pack</Link></div><div className="mt-6"><UsageCard/></div><h2 className="mt-8 text-2xl font-bold">Recent ad packs</h2>{demoPacks.length?<div className="mt-4 grid gap-4 md:grid-cols-2">{demoPacks.map(p=><Link href={`/dashboard/packs/${p.id}`} className="glass rounded-2xl p-5" key={p.id}><p className="text-sm text-slate-400">{p.created}</p><h3 className="mt-2 text-xl font-bold">{p.name}</h3><p className="mt-2 text-slate-300">{p.summary}</p></Link>)}</div>:<EmptyState title="No ad packs yet" body="Create your first product pack to unlock hooks, scripts, captions, audits, and a 7-day plan." href="/dashboard/new" label="Create pack"/>}</div>}
