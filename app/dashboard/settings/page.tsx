import { isSupabaseConfigured } from '@/lib/supabase';import { isStripeConfigured } from '@/lib/stripe';
export default function Settings(){const rows=[['Profile','Demo Seller / demo@adpilot.ai'],['Plan','Starter placeholder'],['Usage','3 ad packs generated this month'],['API status',`Supabase: ${isSupabaseConfigured?'configured':'demo'} • OpenAI: ${process.env.OPENAI_API_KEY?'configured':'mock'} • Stripe: ${isStripeConfigured?'configured':'placeholder'}`],['Billing','Stripe checkout placeholder'],['Danger zone','Account deletion placeholder']];return <div><h1 className="text-3xl font-bold">Settings</h1><div className="mt-6 space-y-3">{rows.map(([k,v])=><div key={k} className="glass rounded-2xl p-5"><h2 className="font-bold">{k}</h2><p className="mt-1 text-slate-300">{v}</p></div>)}</div></div>}
