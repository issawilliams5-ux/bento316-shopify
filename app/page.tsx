import Link from 'next/link';import { FeatureCard, PricingCard } from '@/components/Cards';import { plans } from '@/lib/plans';
export default function Home(){return <main><section className="mx-auto max-w-7xl px-6 py-20 text-center"><div className="mx-auto max-w-4xl"><p className="text-electric">AI creative testing for ecommerce</p><h1 className="mt-4 bg-gradient-to-r from-white via-blue-100 to-violet-300 bg-clip-text text-5xl font-black tracking-tight text-transparent md:text-7xl">Turn any product into 100 ready-to-test ads in minutes.</h1><p className="mx-auto mt-6 max-w-3xl text-xl text-slate-300">Generate hooks, scripts, captions, UGC concepts, product-page fixes, SEO copy, and a 7-day testing plan.</p><div className="mt-8 flex justify-center gap-4"><Link href="/signup" className="rounded-xl bg-electric px-6 py-3 font-bold shadow-glow">Generate My First Ad Pack</Link><Link href="/dashboard/packs/demo-1" className="rounded-xl border border-white/15 px-6 py-3 font-bold">View Demo</Link></div></div></section><section className="mx-auto grid max-w-7xl gap-4 px-6 py-10 md:grid-cols-3">{['50 hooks and UGC scripts','Product-page conversion audit','Swipe file and testing plan'].map((t,i)=><FeatureCard key={t} title={t} body={`Premium workflow ${i+1} built for sellers who need practical creative angles without launching ads for them.`}/>)}</section><section className="mx-auto max-w-7xl px-6 py-16"><h2 className="text-3xl font-bold">How it works</h2><div className="mt-6 grid gap-4 md:grid-cols-3">{['Paste product details','Generate structured pack','Copy, save, export, test'].map(x=><FeatureCard key={x} title={x} body="Clear, organized outputs with demo mode when APIs are missing."/>)}</div></section><section className="mx-auto max-w-7xl px-6 py-16"><h2 className="text-3xl font-bold">Pricing</h2><div className="mt-6 grid gap-4 md:grid-cols-3">{plans.map(plan=><PricingCard key={plan.name} plan={plan}/>)}</div></section><section className="mx-auto max-w-4xl px-6 py-16"><h2 className="text-3xl font-bold">FAQ</h2><div className="mt-6 space-y-3 text-slate-300"><p>Does this launch ads? No — it only creates testing assets.</p><p>Does it scrape competitor ads? No — save inspiration links manually.</p><p>Are results guaranteed? No — use the testing plan to learn responsibly.</p></div></section><footer className="border-t border-white/10 p-8 text-center text-slate-400">© 2026 AdPilot AI</footer></main>}
