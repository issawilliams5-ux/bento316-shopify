import type { AdPack, ProductInput, SwipeItem } from './types';
export const demoInput: ProductInput = { productName:'GlowLift LED Mask', productUrl:'https://example.com/products/glowlift', category:'Beauty', price:'$89', targetCustomer:'busy skincare shoppers over 30', mainProblem:'dull skin and inconsistent routines', mainBenefit:'spa-style glow sessions at home in 10 minutes', keyFeatures:'red light, blue light, flexible silicone, USB-C rechargeable', tone:'UGC' };
const fill = (n:number, cb:(i:number)=>string) => Array.from({length:n},(_,i)=>cb(i+1));
export function mockAdPack(input: ProductInput = demoInput): AdPack { const p=input.productName || 'Your Product'; const audience=input.targetCustomer || 'online shoppers'; const benefit=input.mainBenefit || 'get a better result faster'; return { productSummary:`${p} is a ${input.category || 'ecommerce'} offer for ${audience}. It positions ${benefit} against the pain of ${input.mainProblem || 'slow, frustrating alternatives'} with a ${input.tone} creative angle.`, hooks: fill(50,i=>`${i}. I tried ${p} for 7 days so you do not have to — here is what changed.`), tiktokScripts: fill(10,i=>`Script ${i}: Open with the pain (${input.mainProblem}), show ${p} in hand, demo the top feature, call out ${benefit}, end with “tap to see why shoppers are switching.”`), metaAds: fill(10,i=>`Meta variation ${i}: Headline: Meet ${p}. Primary text: Stop settling for ${input.mainProblem}. Get ${benefit} with features like ${input.keyFeatures}. CTA: Shop now.`), instagramCaptions: fill(10,i=>`Caption ${i}: Your routine called — it wants ${p}. ${benefit}. Save this for your next product upgrade. #ecommercefinds`), staticAdConcepts: fill(10,i=>`Static concept ${i}: Split-screen before/after pain point, bold headline “From ${input.mainProblem} to ${benefit}”, product close-up, 3 feature chips.`), ugcScripts: fill(5,i=>`UGC ${i}: “I was skeptical about ${p}, but the first thing I noticed was...” Show unboxing, quick demo, honest benefit, soft CTA.`), productPageAudit:['Move the core benefit above the fold.','Add three objection-handling bullets near the buy button.','Use social proof beside product imagery.','Add a comparison block against old alternatives.','Clarify shipping, returns, and guarantee language without promising outcomes.'], faqs:['Who is this best for?','How fast will it ship?','What makes it different?','Can I use it daily?','What if it is not right for me?'], seo:{title:`${p} | ${benefit}`, metaDescription:`Shop ${p} for ${audience}. Designed to help with ${input.mainProblem} using ${input.keyFeatures}.`}, testingPlan: fill(7,i=>`Day ${i}: Launch ${i===1?'hook tests':i<4?'script/angle tests':i<6?'caption and static creative tests':'winner consolidation'}, track CTR, saves, CPC, and add-to-cart quality.`)} }
export const demoPacks = [{id:'demo-1', name:'GlowLift LED Mask', created:'Today', summary:'100 ready-to-test angles for a beauty product.'},{id:'demo-2', name:'AeroPack Travel Bag', created:'Yesterday', summary:'Travel hooks, Reels scripts, and product page fixes.'}];
export const demoSwipe: SwipeItem[] = [{adLink:'https://example.com/ad', platform:'TikTok', product:'GlowLift LED Mask', hook:'I tested this for 7 nights...', angle:'Skeptic to believer', notes:'Strong opening pattern.', status:'Idea'}];
