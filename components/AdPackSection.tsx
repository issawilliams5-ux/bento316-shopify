'use client';
import { useState } from 'react';import { CopyButton } from './CopyButton';
export function AdPackSection({ title, items }: { title: string; items: string[] }) { const [saved,setSaved]=useState(false); const text=items.join('\n'); return <section className="glass rounded-2xl p-5"><div className="mb-4 flex flex-wrap items-center justify-between gap-2"><h2 className="text-xl font-semibold">{title}</h2><div className="flex gap-2"><CopyButton text={text}/><button onClick={()=>setSaved(!saved)} className="rounded-lg bg-electric px-3 py-2 text-sm">{saved?'Saved':'Save'}</button><button className="rounded-lg bg-violet px-3 py-2 text-sm">Regenerate</button></div></div><div className="space-y-3">{items.map((item,i)=><p key={i} className="rounded-xl bg-black/20 p-3 text-sm text-slate-200">{item}</p>)}</div></section> }
